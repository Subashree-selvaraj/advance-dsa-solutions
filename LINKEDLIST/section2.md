Question 2.B: Memory-Efficient Dual Traversal
•
•
Part i: Within highly resource-constrained embedded execution environments, demonstrate how a XOR
Linked List circumvents traditional layout overheads to permit full bidirectional traversal utilizing only the
allocation footprint of a single raw pointer field per node. Provide the clear bitwise logical equation utilized to
compute the target successor node address during forward traversal. 
Part i_i: Critically evaluate exactly two (2) distinct operational risks or hardware-level disadvantages
introduced by an XOR linked list implementation with respect to real-time interactive debugging utilities and
automated execution runtimes (e.g., Automatic Garbage Collection tracing).
